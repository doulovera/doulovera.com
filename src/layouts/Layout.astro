<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<title>Douglas Lovera - Frontend Developer</title>
		<script src="https://unpkg.com/phosphor-icons"></script>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--app-min-width: 320px;

		--bg-gradient: linear-gradient(90deg, rgba(57,74,153,1) 35%, rgba(90,108,193,1) 100%);

		/* theme */
		--bg-primary: #292929;
		--bg-secondary: #171717;
		--font-primary: #c5cddb;
		--font-secondary: #ffffff;
		--bg-brand: #5a3fd1;

		--bg-blur: #171717ee;
	}

	* {
		margin: 0;
		padding: 0;
    box-sizing: border-box;
	}

	.brand-background {
    background-color: var(--bg-brand);
	}
</style>

<style>

	html {
		font-family: -apple-system, BlinkMacSystemFont, 'Open Sans', 'Segoe UI', 'Helvetica Neue', sans-serif;
		font-size: 62.5%;
		scroll-behavior: smooth;
	}

	body {
		margin-top: 10rem;
		min-width: var(--app-min-width);
		background-color: var(--bg-primary);
		color: var(--font-primary);
		line-height: 1.5;
		transition: background-color 0.2s;
	}

	main {
		padding: 0 2rem;
	}

</style>
